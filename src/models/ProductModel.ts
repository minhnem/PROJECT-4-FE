export interface ProductModel {
    title: string
    slug: string
    description: string
    categories: string[]
    supplier: string
    images: string[]
    createdAt: string
    updatedAt: string
    isDeleted: boolean
    _id: string
    __v: number
  }